# Release freeze point

**Frozen at:** Tuesday, April 28, 2026 — **11:42:23** (local time, UTC−04:00)

**Repository baseline:** `538d6db` — `fix(home): remove chip row scrollbar — smaller type, wider column`

## What this freeze represents

This marker records the agreed **release / UX baseline** at the timestamp above. Treat **`main`** at **`538d6db`** as the reference snapshot for:

- Home quick-action chips: single row, compact typography, **no horizontal scrollbar** on the chip strip
- Earlier slices on the same train: unified “Recent builds” rail, workspace-first project links, retry-step API, executor preview/template fixes, etc.

## Next steps (operational)

- Tag or branch from this commit if you want an immutable pointer (e.g. `freeze-2026-04-28`).
- Deploy production from **`538d6db`** or later so live matches this baseline.

---

*Document generated as the canonical “freeze point” for this milestone.*
