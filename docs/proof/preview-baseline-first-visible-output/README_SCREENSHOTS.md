# Preview baseline screenshots

This folder is reserved for **browser screenshots** of the first visible preview at:

`/api/preview/tsk_27e3fcf0d496/serve`

None were captured by the freeze automation (no headed browser / URL access from the agent environment).

**Action for humans:** add PNG or WebP files here after opening the preview URL on your machine, then optionally commit them on branch \reeze/first-visible-preview-baseline\.
