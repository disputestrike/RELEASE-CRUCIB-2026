{
  "name": "crucibai-mobile-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile smoke-test scaffolding for CrucibAI (Detox + Jest).",
  "scripts": {
    "test": "jest --config e2e/jest.config.js",
    "e2e:ios": "detox test --configuration ios.sim.debug",
    "e2e:android": "detox test --configuration android.emu.debug"
  },
  "devDependencies": {
    "detox": "^20.0.0",
    "jest": "^29.0.0"
  }
}
