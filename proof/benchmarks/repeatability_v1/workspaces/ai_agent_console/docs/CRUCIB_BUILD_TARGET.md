# CrucibAI — build target for this job

**Agents & automation**

Crew-style sketches, workflow docs, and hooks into the fixed DAG — not a custom LangGraph runtime.

Goal (excerpt): Build an AI agent console with run_agent workflow notes, execution dashboard, operator login, proof panel, and deploy readiness.

## What this run is designed to deliver

- Planning stubs and optional crew pack files under workspace
- Same scaffold as web runs so proofs and preview still apply

## On this run (exactly)

- Automation is represented as files + docs; orchestration remains the Auto-Runner DAG.

## Roadmap (platform breadth — not narrowed)

- User-defined agent graphs, schedules, and external tool connectors.

---
*CrucibAI’s product direction is multi-stack and multi-modal; each Auto-Runner execution mode documents honest guarantees while we expand tracks (Next-native DAG, mobile, deeper automation, etc.).*
