# CrucibAI — build target for this job

**Full-stack web (Vite + React)**

Default Auto-Runner path — SPA, Sandpack preview, FastAPI sketch, SQL, verifiers.

Goal (excerpt): Build an event ticketing app with ticket dashboard, attendee notes, checkout placeholder, organizer team page, and publish proof.

## What this run is designed to deliver

- Vite + React (JS) frontend scaffold with router-ready pages
- Python FastAPI backend sketch and SQL migration stubs
- Workspace preview (Sandpack) and compile-style verification gates

## On this run (exactly)

- Primary app lives at repo root (package.json + src/) as today.

## Roadmap (platform breadth — not narrowed)

- Optional Turbopack / RSC experiments as a separate track.

---
*CrucibAI’s product direction is multi-stack and multi-modal; each Auto-Runner execution mode documents honest guarantees while we expand tracks (Next-native DAG, mobile, deeper automation, etc.).*
