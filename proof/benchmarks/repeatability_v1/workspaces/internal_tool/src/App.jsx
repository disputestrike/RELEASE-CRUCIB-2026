export default function App() {
  return <main><h1>Internal operations tool</h1><p>Build an internal operations tool with auth, task dashboard, team page, persisted operations notes, and deployment readiness.</p><pre>internal
operations
auth
task
deployment</pre></main>;
}
