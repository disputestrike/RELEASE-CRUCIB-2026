# CrucibAI — build target for this job

**API & backend-first**

Emphasizes FastAPI routes, OpenAPI-shaped sketches, and persistence; minimal UI.

Goal (excerpt): Build an inventory management tool with stock dashboard, item notes, team routing, mocked auth, and deploy-ready artifacts.

## What this run is designed to deliver

- Python API sketch with route modules and health-style patterns
- SQL / migration stubs when the goal implies data
- Thin or placeholder UI so preview stays valid

## On this run (exactly)

- Frontend bundle is still generated (for Sandpack) but may stay minimal.

## Roadmap (platform breadth — not narrowed)

- Optional “no frontend step” mode with API-only verification.
- gRPC / GraphQL / eventing packs.

---
*CrucibAI’s product direction is multi-stack and multi-modal; each Auto-Runner execution mode documents honest guarantees while we expand tracks (Next-native DAG, mobile, deeper automation, etc.).*
